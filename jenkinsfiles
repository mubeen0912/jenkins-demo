pipeline {
    agent any

    environment {
        DOCKER_IMAGE = 'mubeenr7/jenkins-demo:latest'
    }

    stages {
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t $DOCKER_IMAGE .'
            }
        }

        stage('Push to Docker Hub') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'dockerhub-credentials',
                    usernameVariable: 'DOCKER_USERNAME',
                    passwordVariable: 'DOCKER_TOKEN'
                )]) {
                    sh 'echo "$DOCKER_TOKEN" | docker login -u "$DOCKER_USERNAME" --password-stdin'
                    sh 'docker push $DOCKER_IMAGE'
                }
            }
        }

        stage('Deploy') {
            steps {
                sh 'docker rm -f jenkins-demo-container || true'
                sh 'docker run -d -p 80:80 --name jenkins-demo-container $DOCKER_IMAGE'
            }
        }
    }
}
